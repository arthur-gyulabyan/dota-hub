import { DraftState, HeroRecommendation } from "../models/types.js";

export const draftStates = new Map<string, DraftState>();
export const recommendations = new Map<string, HeroRecommendation[]>();
